# API Rate Limiter & Gateway

A production-grade rate limiting middleware built with FastAPI and Redis.

## Algorithms
- Fixed Window
- Sliding Window
- Token Bucket (in progress)

## Setup
```bash
pip install -e ".[dev]"
cp .env.example .env
# fill in .env values
```

## Project Status
In active development — Phase 2 of 5 complete.